// Stable surface over the tauri-specta generated client.
// Features import from here, never from ./bindings directly.
export { commands, events } from "./bindings";
export type { Pong, TickEvent } from "./bindings";
